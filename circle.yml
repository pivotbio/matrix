machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t pivot/matrix .

test:
  override:
    - docker run --entrypoint 'bundle exec rspec' pivot/matrix

deployment:
  staging:
    branch: master
    heroku:
      appname: pivot-matrix
